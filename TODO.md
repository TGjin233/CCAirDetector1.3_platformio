# TODO 列表

## 明天任务

### 1. 将 NowAPI 全球指数数据显示到屏幕上

**目标**：在屏幕上显示获取到的全球股票指数（上证指数、深证成指）

**实现步骤**：

1. **创建新的显示页面**
   - 在 `tftUtil.cpp` 中添加 `drawGlobalIndexPage()` 函数
   - 在 `Common.h` 中添加 `GLOBAL_INDEX` 页面枚举
   - 添加页面切换逻辑

2. **定义显示数据结构**
   - 在 `nowAPI.h` 中添加 `IndexData globalIndexData` 全局变量
   - 存储获取到的指数数据

3. **实现屏幕显示函数**
   ```cpp
   void drawGlobalIndexPage() {
     // 显示上证指数
     // - 指数名称
     // - 当前价格
     // - 涨跌幅（带颜色指示：绿色=涨，红色=跌）
     // - 涨跌额
     // - 开盘价、最高价、最低价

     // 显示深证成指
     // - 同上格式
   }
   ```

4. **修改按键逻辑**
   - 在 `Task.cpp` 中添加页面切换支持
   - 长按按钮切换到 GLOBAL_INDEX 页面

5. **整合数据获取**
   - `fetchGlobalIndex()` 获取数据后存储到全局变量
   - 页面刷新时读取并显示

---

## 后续任务（可选）

- [ ] 添加基金数据（006479 中欧远见）显示
- [ ] 添加定时刷新功能
- [ ] 添加指数历史走势简单显示
- [ ] 添加页面切换动画效果

---

## 参考资料

- **现有页面代码**：`tftUtil.cpp` 中的 `drawPage1()`、`drawPage2()` 函数
- **页面切换逻辑**：`Task.cpp` 中的 `btn3LongClick()` 函数
- **颜色定义**：`Common.h` 中的颜色常量

---

## 预计工作量

- 基础显示：约 2-3 小时
- 完善功能：约 1-2 小时
